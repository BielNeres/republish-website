import styled from 'styled-components'

export const Header = styled.div`
    width: 100%;
    position: relative;
    min-height: 70px;
    z-index: 800;
    transition: all 0.3s ease-in-out 0s;
    border-top: 1px solid rgb(56, 52, 69);
`;